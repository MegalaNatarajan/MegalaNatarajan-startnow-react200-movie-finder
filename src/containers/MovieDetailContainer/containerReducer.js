const defaultState = {
  description: '',
  lineItems: []
};

export default function ContainerReducer (state = defaultState, action) {
  // the `state = defaultState` above is new ES6 syntax
  // for defining a default value on a parameter
  return state;
}